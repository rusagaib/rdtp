# rdtp - Radiology Dicom to Pacs

## Backgroud:

i was struggling here to send dicom like before i was manualy export sample dicom file from windows pc to external hdd and then upload it manualy to pacs one-by-one, so i make this crappy code to somewat automate that progress and god it's turn out be trully helpfull..

## Quickstart:

### 1. install python:

### 2. install requirements lib:

```sh
python -m venv venv
pip install -r requirements.txt
```

### 3. manualy run rdtp_service or use .bat file..

### 4. for autostart run app, copy .bat file to folder auto start windows (use `_auto.bat` or `rdtp_service.bat` whatever..)


